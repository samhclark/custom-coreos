name: Cleanup Container Images

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (true/false)'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
  schedule:
    - cron: '0 2 * * 0'  # Weekly on Sundays at 2 AM UTC

env:
  RETENTION_DAYS: 90
  DRY_RUN: ${{ inputs.dry_run || 'false' }}

jobs:
  cleanup:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Plan package-version cleanup
        id: bootc_versions
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: >-
          ./scripts/select-expired-images.sh "${RETENTION_DAYS}"
          --github-output "${GITHUB_OUTPUT}"

      - name: Plan legacy package-version cleanup
        id: legacy_versions
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CONTAINER_PACKAGE_NAME: custom-coreos
        run: >-
          ./scripts/select-expired-images.sh "${RETENTION_DAYS}"
          --github-output "${GITHUB_OUTPUT}"

      - name: Delete old bootc package versions
        uses: actions/delete-package-versions@e5bc658cc4c965c472efe991f8beea3981499c55 # v5
        if: steps.bootc_versions.outputs.delete_versions != '' && env.DRY_RUN != 'true'
        with:
          package-name: 'nas/bootc'
          package-type: 'container'
          package-version-ids: ${{ steps.bootc_versions.outputs.delete_versions }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Delete old legacy package versions
        uses: actions/delete-package-versions@e5bc658cc4c965c472efe991f8beea3981499c55 # v5
        if: steps.legacy_versions.outputs.delete_versions != '' && env.DRY_RUN != 'true'
        with:
          package-name: 'custom-coreos'
          package-type: 'container'
          package-version-ids: ${{ steps.legacy_versions.outputs.delete_versions }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Dry run summary
        if: env.DRY_RUN == 'true'
        run: |
          echo "🧪 DRY RUN COMPLETE"
          echo "This was a dry run - no versions were actually deleted."
          echo ""
          if [[ -n "${{ steps.bootc_versions.outputs.delete_versions }}" ]]; then
            echo "📋 Would delete these nas/bootc versions:"
            echo "${{ steps.bootc_versions.outputs.delete_versions }}" | tr ',' '\n' | nl -w2 -s'. ID: '
            echo ""
          else
            echo "✅ No nas/bootc versions were eligible for deletion"
          fi
          if [[ -n "${{ steps.legacy_versions.outputs.delete_versions }}" ]]; then
            echo "📋 Would delete these custom-coreos versions:"
            echo "${{ steps.legacy_versions.outputs.delete_versions }}" | tr ',' '\n' | nl -w2 -s'. ID: '
            echo ""
          else
            echo "✅ No custom-coreos versions were eligible for deletion"
          fi
          echo "All versions are within the ${{ env.RETENTION_DAYS }}-day retention window"
          echo "To perform actual cleanup, run this workflow with dry_run=false"

      - name: Cleanup completion summary
        if: env.DRY_RUN != 'true' && (steps.bootc_versions.outputs.delete_versions != '' || steps.legacy_versions.outputs.delete_versions != '')
        run: |
          echo "✅ CLEANUP COMPLETED"
          echo "Successfully deleted older container versions"
          echo "Retention policy: ${{ env.RETENTION_DAYS }} days"
