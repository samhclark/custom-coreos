# Remove the obsolete rootful generated config, which contains plaintext
# Pushover credentials. The rootless service reads runtime secret files instead.
r /var/lib/alertmanager/alertmanager.yml - - - -
